name: Java CI

on: [push]

jobs:
  build:
    strategy:
      matrix:
        version: [10, 12]
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - shell: bash
        run: |
          echo ${{ matrix.version }}
          echo ${{ matrix.os }}